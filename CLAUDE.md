# Godot C# project
- Godot 4.x (.NET build), C#, VS Code
- Build check: `dotnet build` from project root
- Scene files (*.tscn) are edited in the Godot editor; ask before modifying them by hand
- Godot C# conventions: classes are `partial`, file name matches class name,
  lifecycle methods are `_Ready`, `_Process(double delta)`, `_PhysicsProcess(double delta)`
- I'm new to C# but know TypeScript — explain language differences when relevant